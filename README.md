# EclipseRefreshAppleScript
You can execute this script from a shell by:
```bash
osascript $PathToScript/eclipseRefresh.applescript
```

The script will change to the Project Explorer and refresh all marked projects.
